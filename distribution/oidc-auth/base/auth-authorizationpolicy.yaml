---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth-allow-in-cluster-redirect
  namespace: istio-system
spec:
  action: ALLOW
  selector:
    matchLabels:
      app: istio-ingressgateway
      istio: ingressgateway
  rules:
  - to:
    - operation:
        hosts:
        - auth.kfprod.honestbank.com
        - dashboard.kfprod.honestbank.com   # needed for redirect after authentication
        - serving.kfprod.honestbank.com   # needed for redirect after authentication
        - '*.serving.kfprod.honestbank.com' # needed for redirect after authentication
        - kubecost.kfprod.honestbank.com # needed for redirect after authentication
---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: auth-allow-access
  namespace: auth
spec:
  action: ALLOW
  rules:
  - {}
  # - from:
  #   - source:
  #       namespaces:
  #       - auth
  #       - istio-system
